language: ruby
rvm:
- 2.1

before_script:
 - chmod +x ./script/cibuild # or do this locally and commit
 - npm install surge 

# Assume bundler is being used, therefore
# the `install` step will run `bundle install` by default.
script: ./script/cibuild

after_success:
<<<<<<< HEAD
  - ./node_modules/.bin/surge --project ./_site --domain huayinthon.surge.sh
=======
  - ./node_modules/.bin/surge --project ./_site --domain sdn.huayinthon.xyz
>>>>>>> 691ed1284a14cffcd6c843dd2d3bd288912ba84b

env:
  global:
  - NOKOGIRI_USE_SYSTEM_LIBRARIES=true # speeds up installation of
